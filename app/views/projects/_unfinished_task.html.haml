%tr
  %td= unfinished_task.title
  %td= unfinished_task.description
  %td= unfinished_task.created_at.strftime('%e %B %Y')
  - if is_the_owner?(@project)
    %td= link_to 'Finish', finish_project_task_path(@project, unfinished_task),
      method: :put
  - else
    %td -
